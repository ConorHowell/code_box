# code_box is a Discord bot that is programmed to take in various commands and respond accordingly. For example, the command Food will print out a randomly selected recipe
# from a list of preset recipes.
